# The package `rene inspect` reports on. Copied to a scratch directory by the
# test, which then edits it to exercise the rebuild conditions.
{
  package = {
    name | String = "graph",
    version = "0.1.0",
  },
}
