# The pipeline's ends, one polyffi unit: stdin parsed into the runtime's
# rc vector on the Rust side, results formatted and printed there too. The
# five-field summary crosses the boundary packed.
{
  package = { name = "vecio", version = "1.1.0" },
}
