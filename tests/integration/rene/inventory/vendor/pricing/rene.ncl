# The money math, the build's second polyffi unit: a generic imported
# scale (instantiated from more than one package — the duplicate textures
# collapse at link) and a four-parameter discount rule that crosses the
# boundary packed.
{
  package = { name = "pricing", version = "2.0.0" },
}
