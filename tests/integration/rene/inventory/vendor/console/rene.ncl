# The report's output, one polyffi unit: every message string lives in the
# Rust textures, so nothing string-shaped crosses the boundary — the
# Reussir side hands over numbers and the formatting happens here.
{
  package = { name = "console", version = "0.3.1" },
}
