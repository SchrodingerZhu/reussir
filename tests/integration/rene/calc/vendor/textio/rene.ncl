# The console package: an opaque `Text` over the runtime's rc string plus
# the imported functions the interpreter reads and reports through. Every
# item here is a polyffi texture — this package is the build's I/O unit.
{
  package = { name = "textio", version = "0.5.2" },
}
