# Rust / Cargo (the workspace target directory lives at the repo root)
/target
crates/**/target
runtime/target
rustc-ice-*
*.profraw
.cargo

# Editors / IDEs
.idea
.vscode
.clice

# Build trees
/build
/build-*
.cache
dist-*

# Environment
.envrc
.direnv

# Misc tooling
_codeql_detected_source_root
node_modules
*.vsix
